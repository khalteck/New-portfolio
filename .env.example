# Optional public Google Analytics 4 measurement ID, for example G-XXXXXXXXXX.
# Analytics remains off until this is configured and the visitor allows it.
VITE_GA_MEASUREMENT_ID=

# Only public, non-secret values may use the VITE_ prefix.
